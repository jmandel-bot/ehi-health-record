node_modules/
bun.lock

# Runtime data (generated by pipeline)
*.db
*.db-wal
*.db-shm
patient_record.json
health_record_compact.json
health_record_full.json
health_record.json
patient_document.json
coverage_report.json

# Custom data directory (when using your own EHI export)
data/custom/

# Legacy symlinks (replaced by data/ submodule + setup script)
tsv
schemas

# reports/ is tracked — it's the audit trail of review/apply cycles
